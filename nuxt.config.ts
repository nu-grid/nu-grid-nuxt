// This config is used for type generation only (nuxi typecheck, dev:prepare)
// The actual runtime configuration is in playgrounds/nuxt/nuxt.config.ts
export default defineNuxtConfig({
  modules: ['@nuxt/ui'],
  compatibilityDate: '2024-07-11',
})
